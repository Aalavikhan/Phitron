#include <stdio.h>
#include <string.h>
int main(void) {
	int n,m;
	scanf("%d %d",&n,&m);
	int arr[100001]={0};
	for (int i=0; i<n; i++){
	    int x;
	    scanf("%d",&x);
	    arr[x]++;
	}
    for (int i=1; i<m+1; i++){
        printf("%d\n", arr[i]);
    }
}

